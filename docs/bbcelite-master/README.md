# BBC Master Elite (bbcelite.com) — agent Markdown mirror

Generated by `scripts/fetch_bbcelite_master_docs.py` from [Elite on the 6502 — BBC Master](https://elite.bbcelite.com/master/).

Use this alongside `intent.md` and the `elite-source-code-bbc-master/` tree.

- Pages converted: **677** under this directory.
- Extra compare pages: **2** under `related/compare/`.

Emulator play links, the general downloads page, Ian Bell’s archive ZIP, BBC Micro Games Archive, and Frontier Astro references are stripped from lists/tables.

Regenerate:

```bash
pip install -r scripts/requirements-fetch-bbcelite.txt
python3 scripts/fetch_bbcelite_master_docs.py
```
